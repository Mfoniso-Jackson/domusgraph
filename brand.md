# Brand — DomusGraph

DomusGraph is a UK housing transparency platform — renters research property history, landlord responsiveness, and maintenance issues via structured, moderated reviews before signing a tenancy.

_Generated by the `brand-design` skill on 2026-09-24. Update by re-running the skill._

## Palette — Verified Slate

**Vibe:** generated · cool minimal · trust
**Category:** consumer / social (closest fit — a UGC trust platform like Trustpilot/Glassdoor, not a raw data-infra product)
**Mood:** premium · minimal

### Seeds

| Role | OKLCH | Hex |
|---|---|---|
| bg-base (`paper`) | `oklch(0.98 0.004 240)` | `#f6f9fb` |
| bg-elevated (`white`) | `oklch(1 0 0)` | `#ffffff` |
| primary (`signal`) | `oklch(0.32 0.05 240)` | `#18364a` |
| secondary text/border (`slate`) | `oklch(0.45 0.03 240)` | `#465865` |
| fg-base (`ink`) | `oklch(0.17 0.015 240)` | `#091015` |
| light fill (`mist`) | `oklch(0.955 0.006 240)` | `#edf1f4` |
| success accent (`leaf`, fixed hue — not brand-derived) | `oklch(0.42 0.06 155)` | `#31573f` |

### Applied to `tailwind.config.ts`, not shadcn

This project predates shadcn/ui — it uses a hand-rolled component library (`components/ui.tsx`) with named Tailwind colors (`ink`, `slate`, `leaf`, `signal`, `paper`, `mist`) defined directly in `tailwind.config.ts`, not shadcn's `--background`/`--foreground` CSS variable system. The brand palette was applied there instead. Backup at `tailwind.config.ts.bak`.

**Important — hex, not `oklch()`, in the config.** Tailwind v3's `@apply` cannot resolve opacity modifiers (`border-slate/20`, `ring-signal/20`) on a raw `oklch()` string in `theme.extend.colors` — it throws a build error. The hex values above are the same colors, converted via the exact OKLCB→sRGB reference math (not an approximation), and are what's actually in the config.

Previous palette was `ink #17201b / moss #385244 / leaf #5c8068 / clay #b25f45 / paper #f7f4ee / mist #e8eee9` (warm terracotta/moss). `clay` was renamed to `signal` and `moss` to `slate` project-wide (17 files) since keeping a color literally named "clay" while it's a blue-gray would be a naming smell.

## Dark mode

Added 2026-09-25. `ink` / `slate` / `leaf` / `signal` / `paper` / `mist` are CSS-variable-backed (`rgb(var(--color-x) / <alpha-value>)` in `tailwind.config.ts`, values defined in `:root` and `.dark` in `app/globals.css`), so every existing usage of these classes — `text-ink`, `border-slate/15`, `bg-mist`, opacity modifiers included — adapts automatically. Dark values are derived from the same seeds (hue-preserved, lightness/chroma adjusted for on-dark contrast), not a naive invert.

A new `surface` token (white in light mode, `#121b22` elevated dark in dark mode) replaced literal `bg-white` for card/panel backgrounds project-wide.

Two tokens are intentionally **fixed** (do not vary with `.dark`): `onyx` (`#091015`) and `graphite` (`#465865`). These back the handful of solid dark CTA blocks — `.button-primary`, the landing-page hero band, the photo-upload file-input button — that stay dark in both themes by design, so their `text-white` always has contrast. Do not use `ink`/`slate` for a block that must always render dark; use `onyx`/`graphite`.

**The header logo is an inline SVG component (`components/logo.tsx`), not a static file rendered via `next/image`.** This is required, not stylistic — `<Image src="/logo.svg">` bakes in the SVG's own hardcoded hex fills, which can't respond to the `.dark` class, and the "Domus" wordmark (fill `#091015`, the light-mode `ink` value) was briefly invisible against a dark header before this was caught. `public/logo.svg` still exists as a standalone downloadable asset but is no longer referenced by the app; any future in-app use of the mark should go through the `Logo` component, not that file.

### Dark seeds

| Role | Light hex | Dark hex | RGB triple (dark, for `--color-*`) |
|---|---|---|---|
| `paper` (page bg) | `#f6f9fb` | `#0b1218` | `11 18 24` |
| `surface` (card bg) | `#ffffff` | `#121b22` | `18 27 34` |
| `mist` (subtle bg) | `#edf1f4` | `#1a242c` | `26 36 44` |
| `ink` (fg text) | `#091015` | `#eef3f6` | `238 243 246` |
| `slate` (muted fg/border) | `#465865` | `#93a5b0` | `147 165 176` |
| `signal` (brand accent) | `#18364a` | `#7fb8e6` | `127 184 230` |
| `leaf` (success accent) | `#31573f` | `#6fd39a` | `111 211 154` |

### Dark contrast check (computed, not approximated)

| Pair | Ratio |
|---|---|
| ink on paper | 16.86:1 |
| ink on surface | 15.58:1 |
| slate on paper | 7.41:1 |
| slate on surface | 6.84:1 |
| signal on paper | 8.89:1 |
| signal on surface | 8.21:1 |
| leaf on paper | 10.29:1 |
| leaf on surface | 9.51:1 |

Every pair clears AA (4.5:1) with room to spare; all clear AAA (7:1) except slate-on-surface, which still clears AA comfortably.

### Toggle behavior

`components/theme-toggle.tsx` is a header button (sun/moon, `lucide-react`) that flips a `.dark` class on `<html>` and persists the choice to `localStorage` (`theme: "light" | "dark"`). An inline script in `app/layout.tsx` (`themeInitScript`, runs synchronously as the first element in `<body>`, before paint) reads `localStorage` and falls back to `prefers-color-scheme` when nothing is stored, so there's no flash of the wrong theme on load. `<html>` carries `suppressHydrationWarning` because this script intentionally mutates `className` before React hydrates.

### Contrast check (all pairs, WCAG AA)

| Pair | Ratio | AA body (4.5:1) | AA large (3:1) |
|---|---|---|---|
| ink on paper | 18.11:1 | ✓ | ✓ |
| ink on white | 19.15:1 | ✓ | ✓ |
| signal on paper | 11.93:1 | ✓ | ✓ |
| signal on white | 12.61:1 | ✓ | ✓ |
| slate on paper | 6.98:1 | ✓ | ✓ |
| slate on white | 7.38:1 | ✓ | ✓ |
| white on ink (primary button) | 19.15:1 | ✓ | ✓ |
| leaf on paper (success icon) | 7.74:1 | ✓ | ✓ |

Every pair clears AA comfortably; most clear AAA (7:1) too.

## Typography — Inter + JetBrains Mono (default, user skipped the picker)

- **Display + body:** Inter
- **Mono (numbers, addresses, postcodes):** JetBrains Mono

Wired via `next/font/google` in `app/layout.tsx`. CSS variables `--font-sans` / `--font-mono`, mapped in `tailwind.config.ts`'s `fontFamily.sans` / `fontFamily.mono`. `<body>` carries `font-sans antialiased`.

### Type scale (recommended, matches current usage)

| Role | Class | Use |
|---|---|---|
| Hero | `text-5xl sm:text-6xl font-bold tracking-tight` | Landing page hero only |
| H1 (page) | `text-3xl sm:text-4xl font-bold tracking-tight` | Page title |
| H2 (section) | `text-xl font-semibold` | Section breaks |
| Body | `text-sm` | Default UI text |
| Reading | `text-base leading-7` | Long-form (terms, privacy) |
| Small / caption | `text-xs text-slate` | Meta, timestamps |
| Mono | `font-mono tabular-nums` | Postcodes, addresses, stats |

## Gradients (not used)

Mood is premium/minimal — gradients would fight the intent. None were generated. Re-run `brand-design` if you want them later.

## Tone and voice

**Use:** plain, concrete, factual language. "Approved" not "greenlit." Numbers and dates over vague claims ("200 properties tracked," not "hundreds of homes"). Say what's verified and what isn't — this is a trust product, so honesty about data completeness matters more than polish (see the homepage's honest empty-state copy, or the Terms page's dispute process).

**Avoid:** hype words ("revolutionary," "game-changing"), vague reassurance ("don't worry," "trust us"), and anything that overstates what's actually verified. Never imply a review is confirmed fact — it's a structured account of one person's experience.

**Voice example:** "Landlord was responsive to a heating issue within 48 hours. Deposit returned in full, no disputes." — specific, factual, no adjectives doing the work that data should do.

## Usage dos and don'ts

**Do:**
- Use the named Tailwind colors (`text-ink`, `bg-signal`, `border-slate/15`) everywhere — never hardcode hex or rgba in components.
- Use `signal` sparingly as the one accent color (CTAs, links, active states) — it's meant to read as *the* brand color, not one of several.
- Use `leaf` only for success/verified states — it's intentionally a fixed hue, separate from the brand-derived family.
- Keep `slate` for secondary text and borders (via opacity, e.g. `border-slate/15`), matching the existing pattern.

**Don't:**
- Don't introduce a second accent hue without updating this file — the whole point of "Verified Slate" is one restrained accent color, not a rainbow of badges.
- Don't add dark mode without first deriving proper dark-mode seeds (see the "No dark mode" note above) — do not just invert the light values.
- Don't reintroduce `clay`/`moss` as color names anywhere — they were deliberately renamed.
